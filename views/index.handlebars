<h1>縮網址</h1>
<form action="/" method="POST">
  <div class="urlInput">
    <input type="text" placeholder="在這裡輸入你的網址" name="originalUrl" required>
    <button type="submit">縮址</button>
  </div>
</form>

{{#if result}}
<div class="urlOutput">
  <label for="urlOutput">你的縮網址為</label>
  <a>{{baseUrl}}{{result.shortenedUrl}}</a>
</div>
{{/if}}